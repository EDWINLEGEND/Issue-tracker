{
  "name": "issue-tracker-system",
  "version": "1.0.0",
  "description": "Real-Time Issue Tracker System with React, Node.js, MongoDB, and Socket.IO",
  "private": true,
  "workspaces": [
    "client",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:client\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "npm run build:shared && npm run build:backend && npm run build:client",
    "build:shared": "cd shared && npm run build",
    "build:backend": "cd backend && npm run build",
    "build:client": "cd client && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../client && npm install && cd ../shared && npm install",
    "start": "cd backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "issue-tracker",
    "real-time",
    "react",
    "nodejs",
    "mongodb",
    "socket.io"
  ],
  "author": "Your Name",
  "license": "MIT"
}